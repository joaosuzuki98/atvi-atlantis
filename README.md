# Executando o projeto

## Instale as dependências

```shell
npm i --legacy-peer-deps # Use a flag caso seja necessário
```

## Rodando o código

```
ts-node teste/index.ts
```

## compilando o código

Se optar por compilá-lo para JS, rode:
```shell
npx tsc
```

O código estará no diretório `out`